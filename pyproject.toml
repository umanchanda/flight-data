[project]
name = "flight-data"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "airportsdata",
    "fastapi",
    "httpx",
    "pandas",
    "psycopg2-binary",
    "python-dotenv",
    "uvicorn",
]
